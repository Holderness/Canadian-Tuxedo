<h1>
	Clothing Items
</h1>

<%= link_to('Add New Item', new_clothing_item_path) %>

<ul>
	<% @clothing_items.each do |item| %>
	<li>
		<div>
			<a href="<%= link_to clothing_item_path(item) %>"><%= item.caption %></a>			
			<%= item.tags %>
		</div>
	</li>
	<% end %>
</ul>

